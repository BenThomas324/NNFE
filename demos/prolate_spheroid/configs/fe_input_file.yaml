
### What do we want to control from the input file? ###

# Path to input file, mesh, and potentially other things...
directory: "."

### FE object ###
# Mesh, quadrature, dim
# Will need to point to a mesh
# Make a special case if someone says cube?
fe_config:
  var: "u"
  mesh_path: "LV.vtk"

  mesh_generator:
    name:
    kwargs:

  kwargs:
    ele_type: "tetrahedron"
    gauss_order: 1
    dim: 3
    vec: 3

### Boundary Condition INFO ###

### Problem object ###
# PDE to solve (mainly Hyperelasticity)
# Material model to use (allow adjusting constants)
# Boundary conditions to apply
# Faces to apply boundary conditions to (tagged mesh)

pde_config:

  pde_info:
    pde_class: "Hyperelasticity"
    pde_type: "Hao_active" # Defines the get_tensor_map
    material_constants:
    internal_vars:
      u:
        fibers:
          value: "mesh"
        sheets:
          value: "mesh"
        normals:
          value: "mesh"
        TCa:
          value: 1.
          vec: 1

  dirichlet_bc_info:
    u:
      bc1:
        type: "Dirichlet"
        component: [0, 1, 2]
        value: [0.0, 0.0, 0.0]
        surface_tag: "base"
        static: True
        
  surface_maps_info:
    u:
      bc2:
        var: ["n", "p"]
        type: "Pressure"
        value: 1.
        surface_tag: "endo"
        static: False

### Solver object ###

solver_config:
  name: "Newton"
  kwargs:
    max_iter: 20
    atol: 1.0e-6

plot_info:
  plot: True
  filename: "output.png"
